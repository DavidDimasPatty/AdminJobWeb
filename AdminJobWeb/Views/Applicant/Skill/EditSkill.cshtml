@model AdminJobWeb.Models.Applicant.Skill;
@{
    Layout = null;
}

<div class="modal-content">
    <form asp-action="EditSkill" asp-controller="Applicant" method="post">
        <div class="modal-header">
            <h5 class="modal-title" id="modalLabel">Edit Skill</h5>
            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
        </div>
        <input type="hidden" name="_id" value="@Model._id" />
        <div class="modal-body">
            <div class="row">
                <div class="col mb-6">
                    <label asp-for="nama" class="form-label" >Nama</label>
                    <input type="nama" asp-for="nama" class="form-control" value="@Model.nama" required>
                </div>
            </div>
        </div>
        <div class="modal-footer">
            <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">Close</button>
            <button type="submit" class="btn btn-primary">Create</button>
        </div>
    </form>
</div>
