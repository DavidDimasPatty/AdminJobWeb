@model IEnumerable<AdminJobWeb.Models.Account.admin>
@{
    ViewData["Title"] = "Admin Dashboard - User Management";
}

<div class="card">
    <div class="card-header d-flex justify-content-between">
        <h5 class="card-title">Administrator List</h5>
        @if (ViewBag.role == 1)
        {
            <form asp-action="SendFormAdmin" asp-controller="User" method="post">
                <button type="submit" class="btn btn-primary btn-sm">
                    <i class="bx bx-plus me-1"></i> Add New Admin
                </button>
            </form>
        }
    </div>
    <div class="card-body">
        <div class="table-responsive text-nowrap">
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th>#</th>
                        <th>Username</th>
                        <th>Email</th>
                        <th>Role</th>
                        <th>Status</th>
                        <th>Action</th>
                    </tr>
                </thead>
                <tbody class="table-border-bottom-0">
                    @{
                        var i = 1;
                        foreach (var item in Model)
                        {
                            <tr>
                                <td>@i</td>
                                <td>@item.username</td>
                                <td>@item.email</td>
                                <td>@item.roleAdmin</td>
                                <td>
                                    @if (item.statusAccount == "Active" && item.statusEnrole)
                                    {
                                        <span class="badge bg-label-success me-1">Active</span>
                                    }
                                    else if (!item.statusEnrole && item.password == null)
                                    {
                                        <span class="badge bg-label-secondary me-1">Creating Password</span>
                                    }
                                    else if (item.statusAccount == "Block")
                                    {
                                        <span class="badge bg-label-danger me-1">Blocked</span>
                                    }
                                    else if (item.password != null && !item.statusEnrole && item.approvalTime == DateTime.MinValue)
                                    {
                                        <span class="badge bg-label-warning me-1">Pending Approval</span>
                                    }
                                    else if (item.approvalTime != DateTime.MinValue && !item.statusEnrole)
                                    {
                                        <span class="badge bg-label-info me-1">Rejected</span>
                                    }
                                </td>
                                <td>
                                    @if (item.password != null && !item.statusEnrole && item.approvalTime == DateTime.MinValue)
                                    {
                                        <form asp-action="ApprovalNewAdmin" asp-controller="User" method="post" class="d-inline">
                                            <input type="hidden" name="id" value="@item._id" />
                                            <button type="submit" class="btn btn-success btn-sm">
                                                <i class="bx bx-check me-1"></i> Approve
                                            </button>
                                        </form>
                                        <form asp-action="RejectNewAdmin" asp-controller="User" method="post" class="d-inline">
                                            <input type="hidden" name="id" value="@item._id" />
                                            <button type="submit" class="btn btn-success btn-sm">
                                                <i class="bx bx-x me-1"></i> Reject
                                            </button>
                                        </form>
                                    }
                                    @if (item.statusAccount == "Active")
                                    {
                                        <a href="@Url.Action("BlockUser", "User", new { id = item._id })" class="btn btn-warning btn-sm">
                                            <i class="bx bx-block me-1"></i> Block
                                        </a>
                                    }
                                    <a href="@Url.Action("Delete", "User", new { id = item._id })" class="btn btn-danger btn-sm">
                                        <i class="bx bx-trash"></i> Delete
                                    </a>
                                </td>
                            </tr>
                            i++;
                        }
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
